{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${desc}",
  "authors": [
    "litetex"
  ],
  "contact": {
    "homepage": "https://github.com/litetex-oss/mcm-${id}",
    "sources": "https://github.com/litetex-oss/mcm-${id}",
    "issues": "https://github.com/litetex-oss/mcm-${id}/issues"
  },
  "license": "${license_spdx_id}",
  "icon": "assets/icon.png",
  "environment": "*",
  "entrypoints": {
    "preLaunch": [
      "net.litetex.authback.common.init.AuthBackCommonInitializer"
    ],
    "client": [
      "net.litetex.authback.client.init.AuthBackClientInitializer"
    ],
    "server": [
      "net.litetex.authback.server.init.AuthBackServerInitializer"
    ]
  },
  "mixins": [
    "authback.mixins.json"
  ],
  "accessWidener": "authback.accesswidener",
  "requires": {
    "fabric-api": "*"
  },
  "recommends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": ">=${minecraft_version}",
    "fabric-api": ">=${fabric_api_version}"
  },
  "custom": {
    "modmenu": {
      "links": {
        "modmenu.github_releases": "https://github.com/litetex-oss/mcm-${id}/releases",
        "modmenu.modrinth": "https://modrinth.com/mod/${id}"
      }
    }
  }
}
